// Helpers to create approval message and verify signature (ethers.verifyMessage).
// Exports: makeApprovalMessage, verifySignature, signatureIsFromCfo
export function makeApprovalMessage(batchId: string, nonce?: string, ts?: number) { /* implement */ }
export function signatureIsFromCfo(message: string, signature: string) { /* implement */ }